[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "synet"
version = "0.1.3"
dependencies = [
    "torch",
    "keras",
    "tensorflow",
    #"yolov5",
    "ultralytics",
]

[tool.setuptools.package-data]
synet = ["zoo/**/*.yaml"]

[project.scripts]
synet = "synet.__main__:main"
